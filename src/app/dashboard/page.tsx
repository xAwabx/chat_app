import { FC } from "react";

interface pageProps {}

const page: FC<pageProps> = ({}) => {
  return (
    <div className="text-center text-xl ">select a chat to view messages</div>
  );
};

export default page;
